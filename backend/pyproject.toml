[project]
name = "backend"
version = "0.1.0"
description = "backend API for ORI Manager"
readme = "README.md"
authors = [
    { name = "aflasso", email = "andresfelipe.lasso@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn[standard]>=0.52.4",
]

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.1.1",
    "ruff>=0.16.6",
]
